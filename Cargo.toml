cargo-features = ["profile-rustflags"]

[workspace]
members = ["platform", "platform_common", "kernel", "kernel_x86_64", "xtask"]

# [profile.dev]
# # For backtraces
# rustflags = ["-Cforce-frame-pointers"]

# [profile.release]
# lto = "thin"
# debug = true
# split-debuginfo = "packed"
